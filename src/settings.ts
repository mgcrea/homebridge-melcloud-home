/** Must match the `platform` key used in Homebridge's config.json. */
export const PLATFORM_NAME = "MELCloudHome";

/** Must match the package name exactly. */
export const PLUGIN_NAME = "@mgcrea/homebridge-melcloud-home";

export const MANUFACTURER = "Mitsubishi Electric";
